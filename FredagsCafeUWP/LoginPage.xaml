<Page
    x:Class="FredagsCafeUWP.LoginPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FredagsCafeUWP"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <Page.DataContext>
        <local:LoginPageViewModel/>
    </Page.DataContext>

    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <StackPanel Orientation="Horizontal" >
                <StackPanel>
                    <TextBox Margin="0,0,20,0" FontSize="25" Height="50" Width="300" PlaceholderText="Username" Text="{Binding UserName,Mode=TwoWay}"/>
                    <PasswordBox Margin="0,0,20,0" FontSize="25" Height="50" Width="300" PlaceholderText="Password" PasswordChar="*" Password="{Binding PassWord,Mode=TwoWay}"/>
                </StackPanel>
                <Button Content="Login" FontSize="25"  Width="100" Height="50" Command="{Binding LoginRelayCommand}"/>
            </StackPanel>
            <TextBlock FontSize="18" FontWeight="Bold" Text="{Binding WrongLogin,Mode=TwoWay}" Foreground="Red"/>
        </StackPanel>
    </Grid>
</Page>
